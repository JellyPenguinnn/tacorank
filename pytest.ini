[pytest]
pythonpath = src
testpaths = tests
addopts = -ra --strict-markers
markers =
    integration: exercises multiple Person 3 subsystems
    failure_injection: verifies bounded failure handling and cleanup
